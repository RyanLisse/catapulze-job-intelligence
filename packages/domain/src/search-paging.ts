/** Rows a search returns when the caller omits `limit`. Shared by the capability window validation (@ji/application) and the search adapter (@ji/search) so the two cannot drift. */
export const DEFAULT_SEARCH_PAGE_SIZE = 20;
