# Copy to apps/worker/.env and fill in real values from the Trigger.dev dashboard.
# Project: catapulze-job-intelligence (TSG-DEV / rjct-lab-d543)
# https://cloud.trigger.dev/orgs/rjct-lab-d543/projects/catapulze-job-intelligence-Yw1m/env/dev

TRIGGER_PROJECT_REF=proj_xgtjezribvfwcmqktcli
TRIGGER_SECRET_KEY=tr_dev_replace_me

# Deploy after setting TRIGGER_SECRET_KEY locally: bun run deploy (from apps/worker)

# Postgres + search (same as apps/server for local smoke)
# Production Trigger cutover (static IPs + FW + DATABASE_URL): docs/runbooks/trigger-on-box-cutover.md
DATABASE_URL=postgresql://ji_app:ji_app_local@127.0.0.1:5432/ji_test
MANTICORE_URL=http://127.0.0.1:9308
# Only relevant while this worker drains inline; must match the search schema.
SEARCH_HYBRID=0

# EffectTS production enablement (CTP-479 / Slice 14) — default OFF.
# Worker canary: JI_EFFECT_WORKER=1 → Effect task-body boundary (Trigger durability unchanged).
JI_EFFECT_WORKER=0
PERF_EFFECT_SPANS=0

# RJC-387: who drains the curated outbox into Manticore after a bron run.
# "worker" (default, unset) is today's behaviour — this process drains
# inline and needs MANTICORE_URL. "onbox" defers entirely to the standalone
# projector process (apps/server, `bun run projector`) running next to
# Manticore, so this worker never needs a route to a private Manticore.
# Production runs onbox — see docs/runbooks/search-projector.md.
# SEARCH_PROJECTOR=onbox

# Local raw-object store for ingest smoke (defaults to .data/raw-objects under
# cwd). Filesystem is used only when RAW_S3_BUCKET is unset (RJC-386) — see
# apps/server/.env.example for the durable S3 backend and how it must match
# whatever the server is configured with (worker and server share one store).
RAW_OBJECT_STORE_PATH=
# RAW_S3_BUCKET=ji-raw-local
# RAW_S3_ENDPOINT=http://127.0.0.1:9002
# RAW_S3_REGION=us-east-1
# RAW_S3_ACCESS_KEY_ID=minioadmin
# RAW_S3_SECRET_ACCESS_KEY=minioadmin

# Per-source egress routing (CTP-602, docs/runbooks/nl-egress.md): route live
# HTTP for sources that block non-NL egress IPs through an operator-run
# forward proxy. Both unset = today's direct egress. Setting sources without
# the URL fails the poller at startup. Never commit a real URL or credentials.
# EGRESS_PROXY_URL=
# EGRESS_PROXY_SOURCES=rabobank,intermediair

# CTP-622: comma-separated source slugs dispatched through curated.durable_job
# (PersistedQueue) instead of an inline poll run. Unset = everything inline.
# Requires migration 0029_durable_job_queue first — see
# docs/runbooks/durable-bron-jobs.md.
# POLLER_DURABLE_BRONNEN=striive

# CTP-626: set to 1 to route enrich-incomplete candidates through the durable
# aanvraag-enrichment queue (curated.durable_job) with an atomic apply. Unset =
# inline enrichment. Same migration prerequisite as POLLER_DURABLE_BRONNEN.
# ENRICHMENT_DURABLE=

# Live connector opt-in (leave unset in CI; fixtures used when unset)
TENDER_NED_LIVE=
INHUURDESK_LIVE=
INTERMEDIAIR_LIVE=
ALLIANDER_LIVE=
ASML_LIVE=
BAM_LIVE=
BIJORANJE_LIVE=
BLUETRAIL_LIVE=
CIRCLE8_LIVE=
CTM_LIVE=
DATAJOBS_LIVE=
ENECO_LIVE=
ENEXIS_LIVE=
ESSENT_LIVE=
GASUNIE_LIVE=
HAERT_LIVE=
FLINTER_LIVE=
FREELANCER_NL_LIVE=
HARVEYNASH_LIVE=
HAYS_LIVE=
HEIJMANS_LIVE=
HERO_LIVE=
INDEED_LIVE=
INDEED_COOKIE=
JOBBIRD_LIVE=
LINKEDIN_LIVE=
MERCELL_LIVE=
NEEDSTAFFING_LIVE=
NS_LIVE=
ONEFELLOW_LIVE=
OPDRACHTOVERHEID_LIVE=
PLANET_INTERIM_LIVE=
PROACT_LIVE=
PROUNITY_LIVE=
RABOBANK_LIVE=
PRORAIL_LIVE=
RANDSTAD_LIVE=
RIJKSWATERSTAAT_LIVE=
STRIIVE_LIVE=
STEDIN_LIVE=
TBI_LIVE=
TECHNIEKWERKT_LIVE=
TENMONKS_LIVE=
TENNET_LIVE=
UNICA_LIVE=
VATTENFALL_LIVE=
VOLKERWESSELS_LIVE=
WERKEN_VOOR_NEDERLAND_LIVE=
WERK_NL_LIVE=
WERKZOEKEN_LIVE=
ZZP_OPDRACHTEN_LIVE=
# Ops Cookie header after a consented browser CF pass (cf_clearance). Never commit real values — see docs/sources/werkzoeken.md (CTP-528).
WERKZOEKEN_COOKIE=


# CTP-653 alert routing: https webhook for opened/escalated alerts (empty = stderr fallback), hours before re-notify.
ALERT_WEBHOOK_URL=
ALERT_ESCALATION_HOURS=4
